name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions

on:
  push:
    branches: main

jobs:

  move-page-content-to-page-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set Git User Info
        run: |
          git config user.email "linhieng@qq.com"
          git config user.name "linhieng"

      - name: test
        run: |
          git checkout --orphan page
          git rm -rf .
          git commit --allow-empty  -m "empty"
          git status
          git push origin page -u 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

